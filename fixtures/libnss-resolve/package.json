{
  "type": "command",
  "name": "libnss-resolve",
  "version": "255.4",
  "platform": "linux_amd64",
  "publisher": "core@globular.io",
  "entrypoint": "bin/libnss-resolve",
  "defaults": {
    "configDir": "",
    "spec": "specs/libnss_resolve_cmd.yaml"
  },
  "description": "systemd-resolved NSS bridge — bundles the upstream libnss-resolve .deb so *.globular.internal hostname resolution works reliably for glibc- and cgo-resolver-based lookups (including Go binaries) without a live apt-get at join/install time.",
  "keywords": [
    "dns",
    "nss",
    "systemd-resolved",
    "resolver"
  ],
  "license": "LGPL-2.1"
}
